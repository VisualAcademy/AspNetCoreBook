@model IEnumerable<string>

@{
    ViewBag.Title = "전체 세션 리스트";
}

<h2>모든 세션</h2>

<ul data-role="listview" data-filter="true" data-inset="true">
    @foreach (var session in Model)
    {
        <li>@session</li>
    }
</ul>
