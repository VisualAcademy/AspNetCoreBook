@model IEnumerable<string>

@{
    ViewBag.Title = "발표자 리스트";
}

<h2>전체 발표자 리스트</h2>

<ul data-role="listview" data-filter="true">
    @foreach (var speaker in Model)
    {
        <li>@Html.ActionLink(speaker, "SessionsBySpeaker", new { speaker })</li>
    }
</ul>
